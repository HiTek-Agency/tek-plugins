// Tek Meet — Service Worker (plan 104-01b SCAFFOLD).
// Plan 104-02 adds WS client + offscreen lifecycle + tabCapture handoff.
self.addEventListener("install", () => {
	console.log("[tek-meet] SW install");
});
self.addEventListener("activate", () => {
	console.log("[tek-meet] SW activate");
});
